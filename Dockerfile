# 使用するPHPのイメージ
FROM php:8.0-cli

# コンテナ内の作業ディレクトリを設定
WORKDIR /usr/src/app

# ホストマシンの現在のディレクトリの内容をコンテナ内にコピー
COPY . .

# PHPビルトインサーバーを起動、publicディレクトリをドキュメントルートとして指定
CMD [ "php", "-S", "0.0.0.0:8080", "-t", "public" ]
